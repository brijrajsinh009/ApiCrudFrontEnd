import { CustomHighlight } from './custom-highlight';

describe('CustomHighlight', () => {
  it('should create an instance', () => {
    const directive = new CustomHighlight();
    expect(directive).toBeTruthy();
  });
});
